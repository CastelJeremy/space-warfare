meta {
  name: /auth/login
  type: http
  seq: 2
}

post {
  url: {{host}}/auth/login
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "username": "JRM",
    "password": "-+"
  }
}
